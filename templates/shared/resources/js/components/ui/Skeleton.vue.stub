<script setup>
defineProps({
  width: { type: String, default: '100%' },
  height: { type: String, default: '1rem' },
  radius: { type: String, default: 'var(--radius-sm)' },
})
</script>

<template>
  <span
    class="block animate-pulse"
    :style="`width: ${width}; height: ${height}; border-radius: ${radius}; background-color: var(--color-surface-muted);`"
    aria-hidden="true"
  />
</template>